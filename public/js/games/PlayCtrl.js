(function()
{
    "use strict";

    angular
        .module("learnary.games")
        .controller("PlayCtrl", ["$scope",
            function PlayCtrl($scope)
            {
                self = this;
            }
        ]);
})();
